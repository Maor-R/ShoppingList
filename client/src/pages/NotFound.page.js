import {PAGE_NOT_FOUND_MSG} from "../constants"

const NotFound = () => {
  return (
    <div className='text-center'>
      <h1>{PAGE_NOT_FOUND_MSG}</h1>
    </div>
  );
};

export default NotFound;
